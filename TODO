# TODO（细化版）
更新时间：2026-04-20
**一切任务都需要受到agent.md内的需求的约束**

## M1：架构与协议先行（P0）
### 1) 落实 agent 端 LangGraph 框架
- [x] 定义 `GraphState`（input/context/resources/result/error/trace）。
- [x] 约定节点生命周期：`prepare -> run -> postprocess -> on_error`。
- [x] 建立节点注册机制（按 `node_type` 映射执行器）。
- [x] 先打通最小链路：`const -> python_snippet -> print`。
- [x] 补齐单测：状态流转、异常分支、并发执行。

### 2) 固化 agent 端执行协议（补充到 `agent.md`）
- [x] 定义前端节点到执行请求的映射字段：
  - `execution_mode`
  - `module.source / function_name / args`
  - `resources`
  - `env`
- [x] 给出 JSON Schema（请求/响应/错误）。
- [x] 约定版本号（如 `protocol_version`）和兼容策略。
- [x] 编写 2~3 个协议示例（最小执行、含资源、含错误回传）。

### 3) 明确 agent 端技术选型
- [x] 输出 ADR：`llama-cpp-python + GGUF + FastAPI` 的选型理由。
- [x] 评估 Redis 队列是否必须（吞吐、削峰、复杂度、运维成本）。
- [x] 列出硬件要求与性能基线（模型大小、内存、QPS、延迟）。

## M2：后端 runtime 能力落地（P0）
### 4) 新增 runtime 路由
- [x] 实现 `GET /api/runtime/capabilities`（模型、资源、限制）。
- [x] 实现 `POST /api/runtime/execute-node`（节点执行主入口）。
- [x] 实现 `GET /api/runtime/health`（依赖健康、模型可用性）。
- [x] 补充 OpenAPI 注释与示例请求/响应。

### 5) 新增 runtime service（动态执行）
- [x] 将前端代码片段写入临时模块并加载执行目标函数。
- [x] 增加超时控制、异常捕获、traceback 结构化输出。
- [x] 返回统一结构：`status / output / logs / error / metrics`。
- [x] 清理临时文件，避免残留与冲突（含并发场景）。

### 6) 建立 `models/` 访问策略
- [x] 仅允许白名单模型名（禁止自由路径）。
- [x] 对模型名做格式校验（字符集、长度、前后缀）。
- [x] 拒绝路径穿越与软链逃逸（`../`、绝对路径等）。
- [x] 记录审计日志（请求方、模型名、结果）。

### 7) 安全隔离
- [x] 执行超时与取消机制（硬超时 + 优雅中断）。
- [x] 资源限制（最大内存、最大并发、队列长度）。
- [x] 限制危险内建与导入（黑名单 + 最小权限）。
- [x] 日志脱敏（token/key/path/user data）。
- [x] 增加安全测试用例（恶意导入、死循环、越权路径）。

## M3：前端接线与兼容（P1）
### 8) 重构节点格式（前端参数化 + 后端代码化）
- [ ] 将“节点定义”和“节点参数”分离。
- [ ] 节点仅在前端暴露可编辑参数，不直接暴露完整执行代码。
- [ ] 保存图时落库：图结构 + 节点参数 + 节点定义版本。
- [ ] 后端按图结构拼接可执行 Python 文件并下发到 agent 端执行。

### 9) 前端接线：新增“后端执行模式”
- [ ] 在节点执行流程增加 `runtime` 模式。
- [ ] 将节点 `code` 与输入打包为 `NodeExecutionRequest`。
- [ ] 执行结果在前端可视化（输出、日志、错误、耗时）。
- [ ] 与现有执行按钮/调试流程保持一致交互。

### 10) 保留兼容：`pyodide` 与 `runtime` 双模式切换
- [ ] 增加全局开关（项目级）与节点级覆盖策略（可选）。
- [ ] 两种模式输出格式对齐，便于对比回归。
- [ ] 增加回退策略：runtime 异常时可一键切回 pyodide。

## M4：RAG 重构与验证（P1）
### 11) 为 agent 端 RAG 选择 pgsql 向量数据库
- [ ] 确认使用 `PostgreSQL + pgvector`。
- [ ] 设计表结构（文档、切片、embedding、metadata、索引）。
- [ ] 编写迁移脚本与初始化脚本。
- [ ] 完成召回评测（准确率、延迟、成本）。

### 12) 重构 agent 端 RAG 结构（满足 `agent.md`）
- [ ] 对齐 `agent.md` 的检索流程与上下文注入规则。
- [ ] 实现数据接入、切片、向量化、召回、重排（如需要）。
- [ ] 增加缓存与失效策略（查询缓存/embedding 缓存）。
- [ ] 增加观测指标（命中率、平均召回耗时、失败率）。

### 13) 端到端验证（至少 4 条链路）
- [ ] `const -> python snippet -> print`。
- [ ] 资源输入链路（`resources` 注入与读取）。
- [ ] 模型推理链路（本地模型加载与推理返回）。
- [ ] 错误回传链路（异常结构化返回到前端）。
- [ ] 每条链路提供可复现脚本与截图/日志留档。

## 时序维度分解（建议 6 周）
时间基准：`T0 = 2026-04-20`（周一）

### Week 1（2026-04-20 ~ 2026-04-26）：协议与骨架冻结
- [x] 完成 M1.1：LangGraph 最小可运行骨架 + 最小链路打通。
- [x] 完成 M1.2：执行协议字段、Schema、示例写入 `agent.md`。
- [x] 完成 M1.3：技术选型 ADR（含 Redis 是否引入结论）。
- [x] 周验收物：`agent.md` 协议初版、ADR 文档、最小链路 Demo。

### Week 2（2026-04-27 ~ 2026-05-03）：runtime 主链路
- [x] 完成 M2.4：`/capabilities`、`/execute-node`、`/health` 三个路由。
- [x] 完成 M2.5：动态执行服务（临时模块加载、结构化返回）。
- [x] 加入基础超时与异常处理，打通后端单节点执行。
- [x] 周验收物：后端 API 可调通，OpenAPI 示例可用。

### Week 3（2026-05-04 ~ 2026-05-10）：安全与可控性
- [x] 完成 M2.6：`models/` 白名单策略与路径安全校验。
- [x] 完成 M2.7：超时/并发/内存限制、危险导入限制、日志脱敏。
- [x] 补齐安全测试用例（恶意导入、死循环、路径穿越）。
- [x] 周验收物：安全基线报告 + 测试结果记录。

### Week 4（2026-05-11 ~ 2026-05-17）：前端 runtime 接线
- [ ] 完成 M3.8：节点定义与参数分离、图保存结构调整。
- [ ] 完成 M3.9：新增“后端执行模式”，打包 `NodeExecutionRequest`。
- [ ] 前端展示统一执行结果（输出/日志/错误/耗时）。
- [ ] 周验收物：前后端联调通过，前端可执行 runtime 模式。

### Week 5（2026-05-18 ~ 2026-05-24）：兼容与端到端闭环
- [ ] 完成 M3.10：`pyodide` 与 `runtime` 双模式切换。
- [ ] 完成 M4.13：4 条端到端链路的首轮验证与问题清单。
- [ ] 对齐两种执行模式输出格式，支持回归对比。
- [ ] 周验收物：E2E 记录、回归对比结果、遗留问题列表。

### Week 6（2026-05-25 ~ 2026-05-31）：RAG 完整落地与收口
- [ ] 完成 M4.11：`PostgreSQL + pgvector` 结构、迁移、初始化。
- [ ] 完成 M4.12：RAG 全流程重构并对齐 `agent.md` 需求。
- [ ] 完成召回评测、性能指标采集与最终验收。
- [ ] 周验收物：RAG 上线方案、评测报告、最终发布清单。

## 关键依赖与并行建议
- [ ] 依赖 A：Week 2 必须基于 Week 1 协议冻结版本推进。
- [ ] 依赖 B：Week 4 前端接线依赖 Week 2 的 API 结构稳定。
- [ ] 依赖 C：Week 6 RAG 联调依赖 Week 5 的双模式与 E2E 基线。
- [ ] 并行建议：Week 3 安全项可与 Week 4 前端接线部分并行，降低总工期风险。

## 横切项（持续执行）
### 14) 中文与编码专项（重点）
- [ ] 新增/修改文档与代码统一 UTF-8（建议 UTF-8 with BOM 用于纯中文文档）。
- [ ] 提交前检查中文字段是否乱码（TODO、README、注释、接口文案）。
- [ ] Git 统一编码与换行策略（`.editorconfig`、`.gitattributes`）。
- [ ] 在 CI 增加编码巡检（检测非法编码/乱码特征）。
